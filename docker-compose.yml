# docker-compose.yml — stack dev local
#
# Sobe o ambiente mínimo para testar o dropin-queue:
#   - nats:    NATS JetStream em modo single-node (sem cluster para dev)
#   - minio:   Object storage S3-compatible para JetStream file storage (futuro)
#   - shim:    Build do dropin-server localmente, conectado ao nats
#
# Uso:
#   docker compose up -d       # sobe tudo
#   make smoke                 # roda smoke test (boto3 cria fila)
#   docker compose logs -f shim
#   docker compose down        # derruba
#   docker compose down -v     # remove volumes também

name: dropin-queue-dev

services:
  # --- NATS JetStream ---
  nats:
    image: nats:2.14.3-alpine
    container_name: dq-nats
    command: ["nats-server", "-js", "-sd", "/data", "-m", "8222"]
    ports:
      - "4222:4222"   # client traffic
      - "8222:8222"   # HTTP monitoring (http://localhost:8222)
    volumes:
      - nats-data:/data
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:8222/healthz"]
      interval: 5s
      timeout: 3s
      retries: 10
      start_period: 5s
    networks:
      - dq-net

  # --- MinIO (S3-compatible object storage) ---
  # Por enquanto apenas provisionado para uso futuro com JetStream
  # File Storage snapshots. O dropin-server não usa ainda.
  minio:
    # Tag pinada — "latest" é não-determinístico e pode quebrar CI/dev
    # silenciosamente em um pull.
    image: minio/minio:RELEASE.2025-04-22T22-12-26Z
    container_name: dq-minio
    command: ["server", "/data", "--console-address", ":9001"]
    environment:
      MINIO_ROOT_USER: minioadmin
      MINIO_ROOT_PASSWORD: minioadmin
    ports:
      - "9000:9000"   # API S3
      - "9001:9001"   # console web
    volumes:
      - minio-data:/data
    healthcheck:
      test: ["CMD", "mc", "ready", "local"]
      interval: 5s
      timeout: 3s
      retries: 10
      start_period: 10s
    networks:
      - dq-net

  # --- dropin-server (AWS API) ---
  shim:
    build:
      context: .
      dockerfile: shim/Dockerfile
      args:
        VERSION: dev-local
    container_name: dq-shim
    depends_on:
      nats:
        condition: service_healthy
      minio:
        condition: service_healthy
    environment:
      GQ_ADDR: ":4566"
      GQ_NATS_URL: "nats://nats:4222"
      GQ_ACCOUNT_ID: "000000000000"
      GQ_REGION: "us-east-1"
      GQ_AUTH_MODE: "off"           # dev only — produção usa strict
      GQ_LOG_LEVEL: "info"
      GQ_SHUTDOWN_TIMEOUT: "10s"
      GQ_STREAM_REPLICAS: "1"       # dev single-node; produção usa 3 (HA)
    ports:
      - "4566:4566"
    networks:
      - dq-net
    # Healthcheck: usa python (instalado no builder, não no runtime)
    # Para healthcheck em produção, configure via orchestrator (k8s/docker)
    # apontando para /healthz e /readyz.

networks:
  dq-net:
    driver: bridge

volumes:
  nats-data:
    driver: local
  minio-data:
    driver: local
